apiVersion: 1

providers:
  - name: StoredSafe
    orgId: 1
    folder: StoredSafe
    folderUid: storedsafe
    type: file
    disableDeletion: true
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
